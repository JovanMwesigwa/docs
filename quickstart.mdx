---
title: 'Quickstart'
description: 'Get up and running with FundKit in under 5 minutes'
---

## Build your first payment integration

This quickstart guide will walk you through setting up FundKit and processing your first payment in just a few minutes.

<Tabs>
  <Tab title="JavaScript/TypeScript">
    <Info>
      **Prerequisites:** Node.js 16+ and npm/yarn/pnpm/bun installed on your machine.
    </Info>

    ## Step 1: Install FundKit

    Install the core package and your chosen providers:

    <CodeGroup>

    ```bash npm
    npm install @fundkit/core @fundkit/honeycoin @fundkit/easypay
    ```

    ```bash yarn
    yarn add @fundkit/core @fundkit/honeycoin @fundkit/easypay
    ```

    ```bash pnpm
    pnpm add @fundkit/core @fundkit/honeycoin @fundkit/easypay
    ```

    ```bash bun
    bun add @fundkit/core @fundkit/honeycoin @fundkit/easypay
    ```

    </CodeGroup>

  </Tab>

  <Tab title="Python">
    <div className="rounded-lg border border-blue-200 bg-blue-50 p-4">
      <div className="flex">
        <div className="flex-shrink-0">
          <svg className="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
            <path fillRule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clipRule="evenodd" />
          </svg>
        </div>
        <div className="ml-3">
          <h3 className="text-sm font-medium text-blue-800">
            Python SDK Coming Soon
          </h3>
          <div className="mt-2 text-sm text-blue-700">
            <p>
              We're actively developing a Python version of FundKit that will provide the same seamless payment processing capabilities.
            </p>
          </div>
        </div>
      </div>
    </div>
  </Tab>
</Tabs>

## Step 2: Get API Keys

<AccordionGroup>
  <Accordion icon="flask" title="Sandbox Keys (Recommended for testing)">
    For testing, you can use FundKit's unified sandbox: ```bash
    FUNDKIT_API_KEY=fk_test_your_sandbox_key_here ```
    <Tip>Sandbox keys let you test immediately without provider approval!</Tip>
  </Accordion>

  <Accordion icon="key" title="Provider Keys (For production)">
    Get API keys from your chosen providers: **HoneyCoin:** - Sign up at
    [honeycoin.io](https://honeycoin.io) - Get your API key and public key from the dashboard
    **EasyPay:** - Register at [easypay.ug](https://easypay.ug) - Obtain your client ID and API
    secret
  </Accordion>
</AccordionGroup>

## Step 3: Initialize the Payment Client

<Tabs>
  <Tab title="JavaScript/TypeScript">
    Create a new file `payment.js` (or `payment.ts` for TypeScript):

    <CodeGroup>

    ```typescript TypeScript
    import { PaymentClient } from '@fundkit/core';
    import { HoneyCoin } from '@fundkit/honeycoin';
    import { EasyPay } from '@fundkit/easypay';

    // Initialize providers
    const honeycoin = new HoneyCoin({
      apiKey: process.env.HONEYCOIN_API_KEY!,
      publicKey: process.env.HONEYCOIN_PUBLIC_KEY!,
    });

    const easypay = new EasyPay({
      apiKey: process.env.EASYPAY_SECRET!,
      clientId: process.env.EASYPAY_CLIENT_ID!,
    });

    // Create payment client
    const client = new PaymentClient({
      apiKey: process.env.FUNDKIT_API_KEY!,
      providers: [honeycoin, easypay],
      environment: 'sandbox',
    });

    export default client;
    ```

    ```javascript JavaScript
    const { PaymentClient } = require('@fundkit/core');
    const { HoneyCoin } = require('@fundkit/honeycoin');
    const { EasyPay } = require('@fundkit/easypay');

    // Initialize providers
    const honeycoin = new HoneyCoin({
      apiKey: process.env.HONEYCOIN_API_KEY,
      publicKey: process.env.HONEYCOIN_PUBLIC_KEY,
    });

    const easypay = new EasyPay({
      apiKey: process.env.EASYPAY_SECRET,
      clientId: process.env.EASYPAY_CLIENT_ID,
    });

    // Create payment client
    const client = new PaymentClient({
      apiKey: process.env.FUNDKIT_API_KEY,
      providers: [honeycoin, easypay],
      environment: 'sandbox',
    });

    module.exports = client;
    ```

    </CodeGroup>

  </Tab>

  <Tab title="Python">
    <Note>
      Python SDK is coming soon! The Python version will have the same simple initialization pattern as the JavaScript SDK.
    </Note>
  </Tab>
</Tabs>

## Step 4: Process Your First Payment

<Tabs>
  <Tab title="JavaScript/TypeScript">
    <CodeGroup>

    ```typescript TypeScript
    import client from './payment';

    async function processPayment() {
      try {
        // Create a payment collection
        const result = await client.collection({
          amount: 10000, // Amount in smallest currency unit (10,000 = 100.00 UGX)
          currency: 'UGX',
          operator: 'mtn',
          accountNumber: '+256701234567',
          externalId: 'tx_' + Date.now(),
          reason: 'Test payment from FundKit'
        });

        console.log('Payment initiated:', {
          provider: result.provider,
          transactionId: result.data.transactionId,
          status: result.data.status
        });

        // Check payment status
        const status = await client.getTransaction({
          provider: result.provider,
          txId: result.data.transactionId,
        });

        console.log('Payment status:', status?.data.status);

      } catch (error) {
        console.error('Payment failed:', error.message);
      }
    }

    processPayment();
    ```

    ```javascript JavaScript
    const client = require('./payment');

    async function processPayment() {
      try {
        // Create a payment collection
        const result = await client.collection({
          amount: 10000, // Amount in smallest currency unit
          currency: 'UGX',
          operator: 'mtn',
          accountNumber: '+256701234567',
          externalId: 'tx_' + Date.now(),
          reason: 'Test payment from FundKit'
        });

        console.log('Payment initiated:', {
          provider: result.provider,
          transactionId: result.data.transactionId,
          status: result.data.status
        });

        // Check payment status
        const status = await client.getTransaction({
          provider: result.provider,
          txId: result.data.transactionId,
        });

        console.log('Payment status:', status?.data.status);

      } catch (error) {
        console.error('Payment failed:', error.message);
      }
    }

    processPayment();
    ```

    </CodeGroup>

  </Tab>

  <Tab title="Python">
    <Note>
      Python SDK is coming soon! Payment processing will be just as simple with the Python version.
    </Note>
  </Tab>
</Tabs>

## Step 5: Run Your Integration

<Tabs>
  <Tab title="JavaScript/TypeScript">
    Set up your environment variables in a `.env` file:

    ```bash .env
    FUNDKIT_API_KEY=fk_test_your_sandbox_key_here
    HONEYCOIN_API_KEY=your_honeycoin_api_key
    HONEYCOIN_PUBLIC_KEY=your_honeycoin_public_key
    EASYPAY_SECRET=your_easypay_secret
    EASYPAY_CLIENT_ID=your_easypay_client_id
    ```

    Then run your code:

    ```bash
    node payment.js
    # or for TypeScript:
    npx tsx payment.ts
    ```

  </Tab>

  <Tab title="Python">
    <Note>
      Python SDK is coming soon! Environment setup will follow Python best practices.
    </Note>
  </Tab>
</Tabs>

## Success!

You should see output similar to:

<Tabs>
  <Tab title="JavaScript/TypeScript">
    ```
    Payment initiated: {
      provider: 'honeycoin',
      transactionId: 'txn_abc123xyz',
      status: 'pending'
    }
    Payment status: completed
    ```
  </Tab>

  <Tab title="Python">
    <Note>
      Python SDK is coming soon! Output format will be similar to the JavaScript version.
    </Note>
  </Tab>
</Tabs>

## Next Steps

<CardGroup cols={2}>

<Card title="Core Concepts" icon="lightbulb" href="/core/payment-client">
  Learn about providers, transactions, and error handling
</Card>

<Card title="Provider Setup" icon="plug" href="/providers/honeycoin">
  Configure specific payment providers
</Card>

<Card title="Error Handling" icon="shield-exclamation" href="/core/error-handling">
  Handle errors gracefully in production
</Card>

<Card title="API Reference" icon="code" href="/api-reference/payment-client">
  Explore the complete FundKit API
</Card>

</CardGroup>

<Warning>
  **Important:** Always use sandbox mode for testing. Switch to production mode only when you're
  ready to process real payments.
</Warning>

## Need Help?

<Card title="Join our Discord Community" icon="discord" href="https://discord.gg/fundkit">
  Get help from other developers and the FundKit team
</Card>
